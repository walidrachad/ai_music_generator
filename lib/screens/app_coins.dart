import 'package:flutter/material.dart';

class AppCoins extends StatelessWidget {
  const AppCoins({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
